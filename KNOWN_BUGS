To expidite your bug report, please specify your machine's OS version
and compiler version.  We can also avoid miscommunication about bugs
if you to ftp a compressed tar file containing the input files and a
script which invokes an example of the bug to /incoming on
qed.rice.edu (128.42.4.38), along with an e-mail note to Rich@Rice.edu
describing it.

Here are known bugs in GNU graphics:

On NeXT cubes, you can safely ignore failure of the regresssion test
for plot2ps. It fails because the default output format for
`printf("%g"...)' is of greater precision than most other runtime
libraries.
